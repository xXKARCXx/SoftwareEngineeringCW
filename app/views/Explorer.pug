extends layout

block content
  - console.log(data) // Debugging output
  .main-content
    h1.Explorer Explorer
    table(border=1)
      thead
        tr
          th Title
          th Description
          th created
      tbody
        each item in data
          tr
            td= item.Title
            td= item.Des
            td= item.created